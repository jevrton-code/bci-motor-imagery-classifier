# Runtime dependencies for bci-motor-imagery-classifier (version 0.1).
# Minimum bounds only - exact versions are intentionally not pinned at this
# scaffold stage. Requires Python 3.11+.
moabb>=1.0
mne>=1.6
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Development / testing (also available via the `dev` optional extra in
# pyproject.toml):
pytest>=7.4
